#include <stdio.h>
#include <cs50.h>

int main(void)
{
    // make the code, run debug50 ./buggy, go through code using step over
    for (int i = 0; i <= 3; i++)  // click on the left of this line to make it a break point
    {
        printf("#\n");
    }
}